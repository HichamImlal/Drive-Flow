import 'package:flutter/material.dart';


class AddPostAdmin extends StatelessWidget {
  const AddPostAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}